import styled from 'styled-components';

const StyledAdminMainHeader = styled.h1`
  font-size: 50px;
  font-weight: normal;
`;

const StyledAdminHeader = styled.h2`
  font-size: 40px;
  font-weight: normal;
`;

export { StyledAdminMainHeader, StyledAdminHeader };
